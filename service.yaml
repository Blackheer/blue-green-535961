apiVersion: v1
kind: Service
metadata:
  name: demo-app-service
spec:
  selector:
    app: demo-app
    release: v2  # Wijzig naar v2 om naar Green deployment te schakelen
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: LoadBalancer
